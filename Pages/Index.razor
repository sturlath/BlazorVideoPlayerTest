@page "/"

<h1>Video below not playing!</h1>


<VideoPlayer.VideoPlayer id="elementId"
                         settings="captions,quality,speed,loop"
                         controls="play-large,restart,rewind,play,fast-forward,progress,current-time,duration,mute,volume,captions,settings,pip,airplay,download,fullscreen"
                         DownloadLink=""
                         Sources="@source"
                         Poster="" />


@code {

    private Dictionary<string, string> source { get; set; }


    protected override async Task OnInitializedAsync()
    {
        source = new Dictionary<string, string>() { { "stream", "https://amssamples.streaming.mediaservices.windows.net/3b970ae0-39d5-44bd-b3a3-3136143d6435/AzureMediaServicesPromo.ism/manifest(format=m3u8-aapl)" } };
    }
}